---
title: Redis
type: system
chapters: [1]
tags: [system, datastore, cache, queue]
status: stub
updated: 2026-05-16
---

# Redis

An in-memory datastore that is also commonly used as a message queue — cited in
[[ch01-reliable-scalable-maintainable]] as an example of category blurring (a
datastore serving queue duties), the mirror image of [[apache-kafka]] (a queue with
datastore durability).

> `status: stub` — expand when later chapters touch in-memory stores / caching.

## Related

- [[apache-kafka]] — the dual example
